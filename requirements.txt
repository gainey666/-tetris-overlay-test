pyyaml>=6.0
opencv-python>=4.8
pygame>=2.5
numpy>=1.24
pybind11>=2.11
mss>=7.0
tqdm>=4.66
onnxruntime>=1.16
Pillow>=10.0

# -------------------------------------------------
# New optional dependencies for the hot-key / window helpers
# -------------------------------------------------
pygetwindow   # window-title detection
keyboard      # global hot-key handling
pywin32>=227  # Windows API access

# Optional tracer (client only)
tracer>=1.0.0
# Optional tracer with Qt UI
# tracer[ui]>=1.0.0
