# Default pipeline – can be overridden with `-p <path>` on the CLI
steps:
  - name: calibrate
    params: {}

  - name: load_config
    params: {}

  - name: run
    params:
      duration: 30           # seconds

  - name: overlay
    params: {}

  - name: benchmark
    params: {}

  - name: shutdown
    params: {}
